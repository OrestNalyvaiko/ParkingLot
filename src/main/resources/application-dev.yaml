spring:
  datasource:
    url: jdbc:mysql://localhost/parkinglot?serverTimezone=UTC&useSSL=false
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    #Show or not log for each sql query
    show-sql: true
    hibernate:
      #Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: update
      #Naming strategy
      #    naming-strategy: org.hibernate.cfg.PhysicalNamingStrategy
  mail:
    host: smtp.gmail.com
    port: 587
    username: orest0998@gmail.com
    password: jdcvfhiwbwevttsf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com
